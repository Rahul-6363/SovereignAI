fastapi==0.111.0
uvicorn[standard]>=0.34.0
pydantic>=2.7
pydantic-settings>=2.2
sqlmodel==0.0.42
SQLAlchemy>=2.0
httpx>=0.27
python-multipart>=0.0.9
pymupdf>=1.24
pillow>=10.0
numpy>=1.26
scikit-learn>=1.3
networkx>=3.2
pytest>=8.0
python-dotenv>=1.0
# deliverable renderers (README Phase 1)
python-docx>=1.1
openpyxl>=3.1

# ── P&ID raster path (README section 4.5 steps 2 and 4) ──
# OCR reads a scanned sheet's text in seconds where a small vision model needs
# minutes per tile; OpenCV recovers the pipe runs and symbol bodies. Both are
# optional -- the pipeline reports them as unavailable and falls back to the
# vision model if they are not installed.
rapidocr-onnxruntime>=1.2
opencv-python>=4.8

# Preferred OCR engine. Reads small, thin and rotated drawing text noticeably
# better than the ONNX default, at the cost of a much heavier install. Fully
# optional: raster_layer falls back to rapidocr when it is absent. Override
# the choice with MESHCORE_OCR_ENGINE=paddleocr|rapidocr.
paddleocr>=2.7
paddlepaddle>=2.6
